# CPU-only PyTorch wheels — much smaller/faster to build than the default
# CUDA-enabled wheels, and this app never needs a GPU.
--extra-index-url https://download.pytorch.org/whl/cpu

Flask==3.1.3
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
psycopg2-binary==2.9.10
python-dotenv==1.0.1
Pillow==12.3.0
torch==2.13.0
transformers==5.15.0
gunicorn==23.0.0
